subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }
}

project(':pi4j-drivers') {
}

project(':rpi-powercontroller') {
    dependencies {
        implementation project(':pi4j-drivers')
    }
}
